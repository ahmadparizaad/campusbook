/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com', 'pin.it', 'i.pinimg.com', 'pinterent.com', 'imgs.search.brave.com', 'images.unsplash.com'],
      },
    };

export default nextConfig;
